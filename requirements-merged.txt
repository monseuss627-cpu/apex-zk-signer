# ApeX/ZK dependencies (platform-specific wheel for x86_64)
apexomni-x86-windows-linux>=3.2.0

# Core web framework
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# HTTP client
httpx>=0.25.0
requests>=2.28.0

# Redis/state management
codewords-client>=0.4.10,<0.5.0

# Blockchain/Web3 dependencies (for on-chain operations)
web3>=6.0.0
eth-account>=0.9.0
ecdsa>=0.18.0

# Math dependencies (pinned for compatibility)
sympy==1.11.1
mpmath>=1.3.0